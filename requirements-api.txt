# Sous-ensemble RUNTIME de requirements.txt pour l'image de l'API (lecture seule).
#
# L'API n'importe que des modules « légers » de pipeline (couleur, synthese,
# jsoncol, ingest.common) : aucune dépendance geopandas / polars / GDAL n'est
# nécessaire au service. On garde donc une image mince (pas de toolchain natif :
# psycopg2-binary et les wheels standard suffisent).
#
# ⚠️ Si un nouvel import de l'API tire une dépendance absente ici, l'ajouter
# (et vérifier qu'elle ne réintroduit pas GDAL sans raison).
fastapi
uvicorn[standard]
SQLAlchemy
psycopg2-binary
alembic
